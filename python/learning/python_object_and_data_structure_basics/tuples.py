# Tuples are very similar to lists. However they have one key difference - immutability.
myList = [1, 2, 3]
print(myList)
t = ('a', 'a', 'b')
print(t.count('a'))
print(t.index('b'))
myList = 'NEW'
print(myList)

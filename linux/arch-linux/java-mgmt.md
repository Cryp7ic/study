If java does not work:

```
sudo archlinux-java fix
```

Check java version:

```
java -version
```

Change java version:

```
Executable (0)
sudo update-alternatives --config java

Compiler (1)
sudo update-alternatives --config javac
```
